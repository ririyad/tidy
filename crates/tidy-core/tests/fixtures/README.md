# Fixtures for tidy-core discovery parsers

Offline unit tests live next to the modules. This folder holds larger golden
HTML/XML samples used by integration-style tests as M1+ grows.

See:
- `feeds.rs` for Atom/RSS parsing tests
- `sitemap.rs` for urlset / sitemapindex tests
- `prefix.rs` for URL prefix matching
